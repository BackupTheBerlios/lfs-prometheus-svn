name=libol
version="0.3.15"

archives=(
	"http://www.balabit.com/downloads/libol/0.3/libol-${version}.tar.gz"
)

function build_pkg {
	./configure --prefix=/usr --enable-shared
	make
}

function install_pkg {
	make install
}
